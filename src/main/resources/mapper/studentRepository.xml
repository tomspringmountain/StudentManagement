<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="raisetech.StudentManagement.repository.StudentRepository">

  <!-- 受講生の全件検索　-->
  <select id="search" resultType="raisetech.StudentManagement.data.Student">
    SELECT * FROM students
  </select>

  <!-- 受講生の検索　-->
  <select id="searchStudent" resultType="raisetech.StudentManagement.data.Student">
    SELECT * FROM students WHERE id =#{id}
  </select>

  <!-- 受講生コース情報の検索(全件)　-->
  <select id="searchStudentCourseList" resultType="raisetech.StudentManagement.data.StudentCourse">
    SELECT * FROM students_courses
  </select>

  <!-- 受講生IDに紐づく受講生コース情報の検索　-->
  <select id="searchStudentCourse" resultType="raisetech.StudentManagement.data.StudentCourse">
    SELECT * FROM students_courses WHERE student_id = #{studentId}
  </select>

  <!-- 受講生の新規登録、IDに関しては自動採番される　-->
  <insert id="registerStudent" parameterType="raisetech.StudentManagement.data.Student"
    useGeneratedKeys="true" keyProperty="id">
    INSERT INTO students (name, kana_name, nickname, email, area, age, sex, remark, is_deleted)
    VALUES (#{name}, #{kanaName}, #{nickname}, #{email}, #{area}, #{age}, #{sex}, #{remark}, false)
  </insert>

  <!-- 受講生コース情報の登録、IDに関しては自動採番される　-->
  <insert id="registerStudentCourse" parameterType="raisetech.StudentManagement.data.StudentCourse"
    useGeneratedKeys="true" keyProperty="id">
    INSERT INTO students_courses (student_id, course_name, course_start_at, course_end_at)
    VALUES (#{studentId}, #{courseName}, #{courseStartAt}, #{courseEndAt})
  </insert>

  <!-- 受講生情報の更新 -->
  <update id="updateStudent" parameterType="raisetech.StudentManagement.data.Student">
    UPDATE students
    SET name = #{name}, kana_name = #{kanaName}, nickname = #{nickname},
    email = #{email}, area = #{area}, age = #{age}, sex = #{sex},
    remark = #{remark}, is_deleted = #{isDeleted}
    WHERE id = #{id}
  </update>

  <!-- 受講生コース情報の更新 -->
  <update id="updateStudentCourse" parameterType="raisetech.StudentManagement.data.StudentCourse">
    UPDATE students_courses
    SET course_name = #{courseName}
    WHERE id = #{id}
  </update>

  <!-- students_courses_statuses の全件取得 -->
  <select id="findAll" resultType="raisetech.StudentManagement.data.StudentsCoursesStatuses">
    SELECT * FROM students_courses_statuses
  </select>

  <!-- student_course_id を指定して status を1件取得 -->
  <select id="findByStudentCourseId" resultType="raisetech.StudentManagement.data.StudentsCoursesStatuses">
    SELECT * FROM students_courses_statuses WHERE student_course_id = #{studentCourseId}
  </select>

  <!-- status を登録 -->
  <insert id="insert" parameterType="raisetech.StudentManagement.data.StudentsCoursesStatuses"
    useGeneratedKeys="true" keyProperty="id">
    INSERT INTO students_courses_statuses (student_course_id, status, updated_at)
    VALUES (#{studentCourseId}, #{status}, #{updatedAt})
  </insert>


  <!-- status を更新 -->
  <update id="update" parameterType="raisetech.StudentManagement.data.StudentsCoursesStatuses">
    UPDATE students_courses_statuses
    SET status = #{status}, updated_at = #{updatedAt}
    WHERE id = #{id}
  </update>

  <!-- status を削除 -->
  <delete id="delete" parameterType="java.lang.Long">
    DELETE FROM students_courses_statuses WHERE id = #{id}
  </delete>

  <!-- mapper.xml -->
<!--  <insert id="registerStudentCourse" useGeneratedKeys="true" keyProperty="id">-->
<!--    INSERT INTO students_courses (student_id, course_name, course_start_at, course_end_at)-->
<!--    VALUES (#{studentId}, #{courseName}, #{courseStartAt}, #{courseEndAt});-->
<!--  </insert>-->

</mapper>